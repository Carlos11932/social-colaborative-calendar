
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>Dashboard - Social Calendar</title>
</head>
<body class="bg-gray-50 font-sans min-h-screen">
    <!-- Loading Screen -->
    <div id="loading" class="flex items-center justify-center min-h-screen">
        <div class="text-center">
            <div class="animate-spin rounded-full h-12 w-12 border-b-2 border-blue-600 mx-auto mb-4"></div>
            <p data-i18n="auth.verifyingAuth" class="text-gray-600 text-lg">Verifying authentication...</p>
        </div>
    </div>

    <!-- Dashboard Content -->
    <div id="dashboard" class="hidden min-h-screen">
        <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
            <!-- Header -->
            <div class="bg-white rounded-lg shadow-sm p-6 mb-8">
                <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between">
                    <div class="mb-4 sm:mb-0">
                        <h1 class="text-3xl font-bold text-gray-900">
                            <span data-i18n="dashboard.welcome">Hello</span>, <span id="user-name">User</span>! 👋
                        </h1>
                        <p data-i18n="dashboard.subtitle" class="text-gray-600 mt-1">Welcome to your social calendar</p>
                    </div>
                    <div class="flex flex-col sm:flex-row sm:items-center gap-3">
                        <span id="user-email" class="text-sm text-gray-500"></span>
                        <button 
                            id="logout-button"
                            data-i18n="auth.signOut"
                            class="bg-red-600 text-white px-4 py-2 rounded-md hover:bg-red-700 focus:outline-none focus:ring-2 focus:ring-red-500 focus:ring-offset-2 transition-colors text-sm font-medium"
                        >
                            Sign Out
                        </button>
                    </div>
                </div>
            </div>

            <!-- Cards Grid -->
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 mb-8">
                <div class="bg-white rounded-lg shadow-sm p-6 border-l-4 border-blue-500">
                    <h3 class="text-lg font-semibold text-gray-900 mb-2">📅 <span data-i18n="dashboard.events.title">Events</span></h3>
                    <p data-i18n="dashboard.events.desc" class="text-gray-600 mb-4">Create and manage your events</p>
                    <button data-i18n="dashboard.events.action" class="bg-blue-600 text-white px-4 py-2 rounded-md hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2 transition-colors text-sm font-medium">
                        Create Event
                    </button>
                </div>
                
                <div class="bg-white rounded-lg shadow-sm p-6 border-l-4 border-green-500">
                    <h3 class="text-lg font-semibold text-gray-900 mb-2">👥 <span data-i18n="dashboard.friends.title">Friends</span></h3>
                    <p data-i18n="dashboard.friends.desc" class="text-gray-600 mb-4">Manage your contacts</p>
                    <button data-i18n="dashboard.friends.action" class="bg-green-600 text-white px-4 py-2 rounded-md hover:bg-green-700 focus:outline-none focus:ring-2 focus:ring-green-500 focus:ring-offset-2 transition-colors text-sm font-medium">
                        View Friends
                    </button>
                </div>
                
                <div class="bg-white rounded-lg shadow-sm p-6 border-l-4 border-gray-300">
                    <h3 class="text-lg font-semibold text-gray-900 mb-2">📊 <span data-i18n="dashboard.stats.title">Statistics</span></h3>
                    <p data-i18n="dashboard.stats.desc" class="text-gray-600 mb-4">Coming soon...</p>
                    <button 
                        disabled 
                        data-i18n="dashboard.stats.action"
                        class="bg-gray-400 text-white px-4 py-2 rounded-md cursor-not-allowed text-sm font-medium"
                    >
                        Soon
                    </button>
                </div>
            </div>

            <!-- Success Message -->
            <div class="bg-green-50 border border-green-200 rounded-lg p-4">
                <div class="flex">
                    <div class="flex-shrink-0">
                        <svg class="h-5 w-5 text-green-400" viewBox="0 0 20 20" fill="currentColor">
                            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
                        </svg>
                    </div>
                    <div class="ml-3">
                        <h3 data-i18n="auth.authWorking" class="text-sm font-medium text-green-800">
                            Authentication working!
                        </h3>
                        <div data-i18n="auth.mvpReady" class="mt-1 text-sm text-green-700">
                            Your MVP is ready for the first functionality.
                        </div>
                    </div>
                </div>
            </div>
        </div>
        
        <!-- Language Switcher -->
        <div class="fixed bottom-4 right-4 bg-white rounded-full shadow-lg p-2">
            <button id="lang-en" class="text-xs text-gray-400 hover:text-gray-600 mr-2 px-2 py-1 rounded">EN</button>
            <span class="text-gray-300">|</span>
            <button id="lang-es" class="text-xs text-blue-600 hover:text-blue-800 ml-2 px-2 py-1 rounded font-semibold">ES</button>
        </div>
    </div>

    <!-- Error Display -->
    <div id="error-message" class="hidden fixed top-4 right-4 bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded max-w-md">
        <span id="error-text"></span>
    </div>

                <script type="module">
            import '/src/scripts/dashboard.ts';
        </script>
</body>
</html>